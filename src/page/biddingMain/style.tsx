import { styled } from "#/stitches.config";
import { Hexile } from "@/component";

export const WaitingBidProductWrapper = styled(Hexile, {
  accentBorder: true,
  accentShadow: true,
  padding: "3rem",
});
