import { styled } from "#/stitches.config";
import { Hexile } from "@/component";

export const UpcomingBidProductWrapper = styled(Hexile, {
  accentBorder: true,
  accentShadow: true,
  padding: "3rem",
});
